# 2026-07-30

## Merged upstream `dsebastien/obsidian-update-time` (1.2.1 -> 1.5.0)

Synced the fork with upstream using `git merge -X ours upstream/main`. Conflicts auto-resolved in favor of local; local authority preserved for the delayed-active-note-write + immer-removal work (`cddfbce`).

### What came in from upstream

- **What's New tab** (1.3.0 -> 1.5.0): replaces modal dialog with a per-plugin `<plugin-id>-whats-new` view; aggregates across plugins on bulk update; support CTAs (Knowii, GitHub Sponsors, Buy Me a Coffee).
  - New: `src/app/whats-new.ts`, `src/app/ui/whats-new-view.ts`, `src/app/ui/support-links.ts`, `src/app/utils/release-notes.ts`, `src/app/utils/whats-new-identity.ts` (+ specs).
- **Excluded folders fix** (1.2.3, issue #9): `isInIgnoredFolder` path-segment-aware matcher; settings UI rejects blank entries.
  - New: `src/app/utils/is-in-ignored-folder.fn.ts` (+ spec), `src/app/settingTab/index.spec.ts`, `src/app/utils/folder-suggest.ts` changes.
- **Build/CI**: two-phase release attestation, obsidian typings pinned to 1.12.0, install into manifest-id folder.
- `manifest.json`: version 1.5.0, `minAppVersion` 1.8.7, `fundingUrl` now multi-link object.
- Docs: CHANGELOG, release-notes page, support CTAs in README/docs.

### Merge conflict resolution

`-X ours` kept local content on every conflicting hunk. One artifact needed a manual fix: `src/app/plugin.ts` called `isInIgnoredFolder(...)` (upstream call site survived) but the import line was dropped (local won that hunk - local had inline `startsWith` logic). Added `import { isInIgnoredFolder } from './utils/is-in-ignored-folder.fn'`. The upstream function is a strict improvement (path-segment aware, skips blanks) over the old inline check, so adopting it is correct.

### Verification

- `tsc --noEmit`: pass (after the import fix).
- `eslint . --max-warnings 0`: pass, zero warnings.
- `bun scripts/build.ts --prod`: pass (`main.js`, `styles.css`, assets emitted).
- `bun test`: 87 pass, 2 fail, 1 unhandled error. **Failures are pre-existing** (both files exist identically in `cddfbce` before the merge, unrelated to merge changes):
  - `src/app/utils/front-matter-writer.spec.ts`: imports `TFile` from `obsidian` at runtime; `obsidian` is types-only ⇒ `SyntaxError`.
  - `src/app/utils/parse-date.fn.spec.ts`: `parseDate('2024', 'yyyy')` returns null.
- bun 1.3.14 installed locally (`C:\Users\Administrator\.bun\bin\bun.exe`) to run the toolchain.

### Open questions / follow-ups

- The two pre-existing test failures predate this merge and should be fixed in a separate change.
- **Manual runtime check needed** (no live vault): verify the What's New tab opens on first load after the version jump to 1.5.0, and that excluded-folders settings still add/persist correctly with the local writer/debounce logic.
